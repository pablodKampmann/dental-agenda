import { db } from "../firebase";
import { ref, set, get } from "firebase/database";

export async function setAppointment(patientId: Number, dentistId: Number, fromDate: Date, untilDate: Date) {
    
}

